# ── HUMAN REVIEW ──────────────────────────────────────────
# Reviewer: Tucker Paron
# Date: 2026-05-05
# Changes from AI draft: None
# Notes:
#   - requires settin up actual S3 bucket and getting OIDC IAM identity for secrets
# ──────────────────────────────────────────────────────────

name: Deploy

on:
  push:
    branches: [main]

env:
  AWS_REGION: us-east-1

jobs:
  test:
    name: Run API tests and validate Terraform

    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4.2.2

      - uses: actions/setup-python@v5.6.0
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install fastapi uvicorn httpx pytest psycopg2-binary

      - name: Run tests
        run: pytest services/api/tests/ -v

      - name: Install Terraform
        uses: hashicorp/setup-terraform@v3

      - name: Validate Terraform
        run: terraform -chdir=terraform init -backend=false && terraform -chdir=terraform validate

  check-aws:
    name: Check for AWS credentials
    runs-on: ubuntu-latest
    needs: test
    outputs:
      configured: ${{ steps.check.outputs.configured }}
    steps:
      - id: check
        env:
          AWS_ROLE_ARN: ${{ secrets.AWS_ROLE_ARN }}
        run: |
          if [ -n "$AWS_ROLE_ARN" ]; then
            echo "configured=true" >> $GITHUB_OUTPUT
          else
            echo "AWS_ROLE_ARN not set — skipping build and deploy"
            echo "configured=false" >> $GITHUB_OUTPUT
          fi

  build-local:
    name: Build Docker images (local path)
    runs-on: ubuntu-latest
    needs: [test, check-aws]
    if: needs.check-aws.outputs.configured == 'false'
    steps:
      - uses: actions/checkout@v4.2.2

      - name: Create .env from example
        run: cp .env.example .env

      - name: Build all images via Docker Compose
        run: docker compose build

  build-and-push:
    name: Build and push images to ECR (prod path)
    runs-on: ubuntu-latest
    needs: [test, check-aws]
    if: needs.check-aws.outputs.configured == 'true'
    permissions:
      id-token: write  # required for OIDC auth to AWS
      contents: read

    steps:
      - uses: actions/checkout@v4.2.2

      - name: Configure AWS credentials via OIDC
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Log in to ECR
        id: ecr-login
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build and push API image
        env:
          ECR_REGISTRY: ${{ steps.ecr-login.outputs.registry }}
          IMAGE_TAG: ${{ github.sha }}
        run: |
          docker build \
            -f services/api/Dockerfile \
            -t $ECR_REGISTRY/hks-prod-api:$IMAGE_TAG \
            -t $ECR_REGISTRY/hks-prod-api:latest \
            ./services/api
          docker push $ECR_REGISTRY/hks-prod-api:$IMAGE_TAG
          docker push $ECR_REGISTRY/hks-prod-api:latest

      - name: Build and push pipeline image
        env:
          ECR_REGISTRY: ${{ steps.ecr-login.outputs.registry }}
          IMAGE_TAG: ${{ github.sha }}
        run: |
          docker build \
            -f services/pipeline/Dockerfile \
            -t $ECR_REGISTRY/hks-prod-pipeline:$IMAGE_TAG \
            -t $ECR_REGISTRY/hks-prod-pipeline:latest \
            .
          docker push $ECR_REGISTRY/hks-prod-pipeline:$IMAGE_TAG
          docker push $ECR_REGISTRY/hks-prod-pipeline:latest

  deploy:
    name: Deploy to ECS
    runs-on: ubuntu-latest
    needs: [build-and-push, check-aws]
    if: needs.check-aws.outputs.configured == 'true'
    permissions:
      id-token: write
      contents: read

    steps:
      - uses: actions/checkout@v4.2.2

      - name: Configure AWS credentials via OIDC
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Force ECS to pull new images
        run: |
          aws ecs update-service \
            --cluster hks-prod-cluster \
            --service hks-prod-api \
            --force-new-deployment

          aws ecs update-service \
            --cluster hks-prod-cluster \
            --service hks-prod-pipeline \
            --force-new-deployment
